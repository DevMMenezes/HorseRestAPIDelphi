unit Utils;

interface

uses
  SysUtils;

procedure SaveLog(sMsg: String);

implementation

uses
  uMain;

{ TUtils }

class procedure TUtils.SaveLog(sMsg: String);
begin
  with uFrmMain, mmLog do
  begin
    Lines.Add(DateTimeToStr(Now) + ' - ' + sMsg);
  end;
end;

end.
